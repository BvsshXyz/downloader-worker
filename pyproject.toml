[project]
name = "downloader-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "httpx",
  "beautifulsoup4"
]

[dependency-groups]
dev = [
  "workers-py>=1.90",
  "workers-runtime-sdk"
]
